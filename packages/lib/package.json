{
  "name": "@doubles-member-generator/lib",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest",
    "lint": "eslint",
    "format": "prettier --write '**/*.{js,ts,md,json}'",
    "typecheck": "tsc --noEmit"
  }
}
